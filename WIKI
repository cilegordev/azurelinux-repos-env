    About 200++ packages

    A. In this repo you can see, my commits are usually just simple descriptions 
    like rebuild, update, new package addition, optimization, or hotfix. Why?
    Because I package and debug everything alone without any feedback. There’s a 
    lot that needs adjusting, or sometimes I just forget to input a small piece of
    code. If I force an upgrade to a new release in the .spec, there would be way
    too many versions coming out, and they’d only be small improvements based on
    my debugging experience. That’s very inefficient, so I prefer to do rebuilds.

    B. There are some STANDALONE packages. What are those? They’re packages
    patched to be compatible with the DE/WM being used — especially for crucial
    parts like drivers.

    C. The packages I provide are quite limited because I tend to repeat or refine
    what already exists instead of wrapping too many packages with weak
    improvements. If the latest commit status says rebuild, just know that I’m 
    perfecting it so that it works flawlessly on a fresh install — but not
    necessarily with upgrades, huffft.

    D. If you need a fast way to get Xfce or Hyprland preconfigured, you can 
    install azurelinux-desktop-xfce-packages or 
    azurelinux-window-hyprland-packages. These are meta-packages that can turn it
    into a ready-to-use Linux system.
    
    E. Back to D if you install on vbox, vmware or kvm install 
    azurelinux-gui-vm-packages. this package is core of xfce and use xinit,
    fbdev and vesa. so no need gpu driver.
    
    F. News maintenance not bug just reminder for me
    1. can't change auto bash to zsh 
    2. imposible to auto icons.screen0-<res>.rc 
    3. i don't know to fix lid close blinking on xfce4-power-manager
    
    G. News kernel 6.6.12.1 prepatch linux-surface for surface devices base on 
    azurelinux kernel
    
    H. Library tools
    │   ├── cups-filters-2.0.1-1.azl3.x86_64.rpm (wpsoffice)
    │   ├── freerdp3-3.16.0-1.azl3.x86_64.rpm (winboat)
    │   ├── kazam-2.0.0-1.azl3.x86_64.rpm (screenrecorder)
    │   ├── kernel-drivers-extras-6.6.117.1-2STANDALONE.azl3.x86_64.rpm (driver)
    │   ├── libxdo-3.20211022.1-1.azl3.x86_64.rpm (rustdesk)
    │   ├── mesa-dri-drivers-extras-24.0.1-5STANDALONE.azl3.x86_64.rpm (driver)
    │   ├── NetworkManager-1.51.4-2.azl3.x86_64.rpm (favorite)
    │   ├── neofetch-7.1.1-1.azl3.x86_64.rpm (exclsv)
    │   ├── parole-4.18.1-1.azl3.x86_64.rpm (effort)
    │   ├── polkit-123-4STANDALONE.azl3.x86_64.rpm (danger)
    │   ├── qtbase-xcb-6.6.3-5STANDALONE.azl3.x86_64.rpm (calamares)
    │   ├── upower-0.99.11-6STANDALONE.azl3.x86_64.rpm (first standalone package)
